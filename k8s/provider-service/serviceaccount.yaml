apiVersion: v1
kind: ServiceAccount
metadata:
  name: provider-service-sa
  namespace: app
  # No IRSA annotation — provider-service uses RDS via K8s Secret credentials,
  # no direct AWS SDK calls requiring IAM role.
