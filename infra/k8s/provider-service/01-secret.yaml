apiVersion: v1
kind: Secret
metadata:
  name: provider-service-secret
  namespace: app
type: Opaque
stringData:
  AWS_REGION: "ap-southeast-1"
  RDS_HOST: "REPLACE_RDS_HOST"
  RDS_PORT: "5432"
  RDS_DATABASE: "providers_db"
  RDS_USER: "REPLACE_RDS_USER"
  RDS_PASSWORD: "REPLACE_RDS_PASSWORD"
